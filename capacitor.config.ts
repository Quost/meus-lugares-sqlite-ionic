import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'A06-meus-lugares',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
